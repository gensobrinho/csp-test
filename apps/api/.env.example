PORT=8000

# Local Docker Postgres (docker compose up -d)
# user/password/db must match docker-compose.yml
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/csp_kanban?schema=public

JWT_SECRET=change-me-to-a-long-random-secret
JWT_EXPIRES_IN=8h
BCRYPT_SALT_ROUNDS=10
