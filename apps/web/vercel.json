{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "cd ../.. && corepack enable && pnpm install --frozen-lockfile --filter @csp-test/web...",
  "buildCommand": "pnpm run build",
  "outputDirectory": "dist"
}
